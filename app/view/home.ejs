<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>首页</h1>
    <% if (isLogin) { %>
        <div>
            用户信息：
            <img src="<%= ctx.user.photo %>" alt="" width="200px">
            <span><%= ctx.user.displayName %> </span>
            <%= JSON.stringify(ctx.user) %>
        </div>
        <a href="/loginout"></a>
    <% } else { %>
        <a href="/passport/github">使用github登录</a>
    <% } %>
</body>
</html>